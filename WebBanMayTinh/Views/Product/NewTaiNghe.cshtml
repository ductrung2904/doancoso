@model List<WebBanMayTinh.Models.ProductModel>


<div class="owl-carousel homepromo swatch hot-item" style="height:auto;">
    @foreach (var item in Model)
    {
        var url = "/chi-tiet/" + item.MetaTitle + "-" + item.ID;
        <div class="item">
            <a href="@url" class="" vertion2020 large">
                <img width="180" height="180" src="@item.Images" class="lazy" alt="" />
                <h3>@item.Name</h3>

                <div class="price">
                    <strong>@(item.Price.HasValue ? item.Price.Value.ToString("N0") + "đ" : "Liên hệ")</strong>
                </div>
                <div class="ratingresult">
                    <i class="icon-ystar"></i>
                    <i class="icon-ystar"></i>
                    <i class="icon-ystar"></i>
                    <i class="icon-gstar"></i>
                    <i class="icon-gstar"></i>
                    <span>9 đánh giá</span>
                </div>
            </a>
        </div>
    }
</div>

